# Sprint 2.7 Repository Inventory

1. `PostgresMockTemplateRepository` — Persistence for `mock_templates` and `mock_template_sections`.
2. `PostgresMockSessionRepository` — Persistence for `mock_sessions` state machine.
3. `PostgresMockAttemptRepository` — Persistence for `mock_attempts` and `mock_attempt_answers`.
4. `PostgresMockResultRepository` — Persistence for `mock_results` and `mock_section_scores`.
5. `PostgresMockReportRepository` — Persistence for `mock_reports`.
6. `PostgresMockReadinessRepository` — Persistence for `mock_readiness` calculations.
