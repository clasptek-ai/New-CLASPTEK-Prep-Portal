import React from 'react';
import { BookmarkButton } from './BookmarkButton';

export default {
  title: 'Assessment/BookmarkButton',
  component: BookmarkButton,
};

export const Default = () => <BookmarkButton isBookmarked />;
