'use client';

import React from 'react';
import { Card } from '../../../components/ui/ui-components';

export function QuestionBankScreen() {
  return (
    <Card title="Question Bank Explorer">
      <p style={{ margin: 0, fontSize: '0.85rem', color: '#cbd5e1' }}>
        Preview questions templates, search tags, test psychometrics, and verify review stages.
      </p>
    </Card>
  );
}
export default QuestionBankScreen;
